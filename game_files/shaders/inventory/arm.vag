#version 330 core
layout (location=0) in vec3 aPos;
layout (location=1) in vec2 aUV;

out V_OUT{
	vec3 pos;
	vec2 uv;
} vData;

uniform vec3 arm_offset;

void main()
{
	vData.pos=aPos+arm_offset;
	vData.uv=aUV;
	gl_Position=vec4(0,0,0,1);
}