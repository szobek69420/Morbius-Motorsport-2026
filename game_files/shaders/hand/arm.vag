#version 330 core
layout (location=0) in vec3 aPos;
layout (location=1) in vec2 aUV;

out V_OUT{
	vec3 pos;
	vec2 uv;
} vData;

uniform mat4 model;

void main()
{
	vData.pos=vec3(model*vec4(aPos,1));
	vData.uv=aUV;
	gl_Position=vec4(0,0,0,1);
}