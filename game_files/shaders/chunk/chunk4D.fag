#version 460 core
in vec3 normal;
in vec3 uv;

out vec4 fragmentColour;

layout (binding=0) uniform sampler2DArray blockTextures;

uniform vec3 sunDirection;

void main()
{
	fragmentColour=(0.4+0.6*max(0.0, dot(normal, sunDirection)))*texture(blockTextures, uv);
}