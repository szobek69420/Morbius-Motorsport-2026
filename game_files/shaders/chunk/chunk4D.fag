#version 460 core
in vec3 normal;
in vec2 uv;

out vec4 fragmentColour;

uniform sampler2D albedo;
uniform sampler2D specular;

const vec3 sunDirection = vec3(0.36, 0.8, 0.48);

void main()
{
	fragmentColour=(0.4+0.6*max(0.0, dot(normal, sunDirection)))*texture(albedo, uv);
}