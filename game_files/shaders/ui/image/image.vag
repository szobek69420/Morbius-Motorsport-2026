#version 460 core
layout (location=0) in vec2 vertexData;

out vec2 uv;
out vec2 pos;

uniform vec2 position;
uniform vec2 scale;
uniform mat4 pv;

void main()
{
	uv=vertexData;
	pos=position+vertexData*scale;
	gl_Position=pv*vec4(pos, 0.0, 1.0);
}